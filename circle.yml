machine:
  java:
    version: oraclejdk8

dependencies:
  pre:
    - cd /home/ubuntu/jgrades/jg-bom
    - mvn clean install
    - cd /home/ubuntu/jgrades/

  override:
    - mvn clean install -Pprod -fae

